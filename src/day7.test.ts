import { parseDay7, contains } from './day7'

const input = `shiny gold bags contain 2 dark red bags.
dark red bags contain 2 dark orange bags.
dark orange bags contain 2 dark yellow bags.
dark yellow bags contain 2 dark green bags.
dark green bags contain 2 dark blue bags.
dark blue bags contain 2 dark violet bags.
dark violet bags contain no other bags.`

test.skip('parse', () => {
    // const parsedData = parseDay7(input)
    // contains(parsedData)
    // console.log(parsedData)
    // expect(contains(parsedData)).toBe(127)
    expect(1).toBe(1)

})

// test.skip('parse full set', () => {
//     const parsedData = parseDay7(day7Data)
//     // contains(parsedData)
//     expect(contains(parsedData)).toBe(4)

// })